# Server Configuration
PORT=3000

# Public URL where server is accessible (for image URLs in webhook)
# Example: https://your-domain.com or http://localhost:3000
PUBLIC_URL=http://localhost:3000

# Webhook Configuration (optional)
# If set, server will POST results to this URL after processing
WEBHOOK_URL=

# API Key for webhook authentication (optional)
# Will be sent as "x-api-key" header
WEBHOOK_API_KEY=
